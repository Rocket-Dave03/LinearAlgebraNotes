# Basic Variable
If a column of $R$ contains a [[Echelon Forms#Leading Entry|leading entry]] then the variable corresponding to that column is called a *basic variable*

# Free Variable
If a variable is not basic, then is is called a non-basic or *free variable*
